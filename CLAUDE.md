# CLAUDE.md — 동화 캐릭터 스토리 웹 프로젝트

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 가이드입니다. 기획 배경은 `동화 캐릭터 스토리 웹 기획안` 문서(WrightHere 논문 참고) 참고.

## 프로젝트 한 줄 요약

9~12세 어린이가 동화 맵을 선택해 그 세계 속 캐릭터와 대화하며 이야기를 만들어가는 웹. 새 캐릭터는 "도감에서 선택"하거나 "직접 그려서 카메라로 스캔"해서 등장시킬 수 있다(그림 스캔은 선택 기능).

## 확정된 설계 결정 (바꾸지 말 것)

- **세계관**: 오마주 캐릭터(전래동화 원작 서사 재해석) + 오리지널 맵 아트. **특정 스튜디오의 비주얼 디자인(캐릭터 얼굴·의상·색감)은 절대 모방하지 않는다** — 서사 구조만 차용하고 아트는 완전 오리지널로 제작.
- **캐릭터 추가는 두 경로가 항상 동등하게 존재**:
  1. 캐릭터 도감에서 선택 (즉시 배치, 지연 없음)
  2. 직접 그려서 카메라로 스캔 → 2D 스프라이트化 (선택 기능, 필수 관문 아님)
- **캐릭터 시각 형태는 2D 스프라이트**로 확정 (3D 메쉬 아님). 파츠 분리(팔다리 리깅) 없이 통짜 이미지 + procedural 애니메이션(bounce, squash&stretch, flip)으로 시작.
- **맵은 지금 단계에서 고정(사전 제작) 맵만 사용한다.** 실시간 3D 생성은 물론, 프롬프트로 맵을 생성하는 기능도 지금은 만들지 않는다(WrightHere와의 의도적 차이) — 미리 그려둔 배경 일러스트 + 씬 전환으로 구현.
- **스토리는 자유 텍스트 누적에만 머무르지 않는다.** 플레이하면서 아이가 선택 가능한 갈림길(2~3개 선택지 형태의 대사/행동)을 제공해, 대화가 실제로 스토리 분기로 이어지는 구조로 확장한다.

## 기술 스택

| 영역 | 선택 |
| --- | --- |
| 렌더링 | Pixi.js (2D 스프라이트/맵) |
| 맵 | 사전 제작 2D 배경 일러스트 + 씬 전환 |
| 배경 제거(그림 스캔용) | rembg 등 오픈소스 세그멘테이션 |
| 대화 AI | LLM + 페르소나 프롬프트(동화 캐릭터=고정 페르소나, 내 캐릭터=아이 설정 반영) + 아동 안전 필터 레이어 |
| 음성 | TTS (캐릭터별 목소리 프리셋) |
| 백엔드 | 세션별 스토리/캐릭터 저장 + 추후 부모 대시보드용 로그 |

## 개발 순서 (반드시 이 순서로 진행)

캐릭터 그림 스캔은 지연/실패 리스크가 있는 기능이므로, **도감 기반 핵심 루프를 먼저 완성하고 그림 스캔은 나중에 얹는다.**

### Phase 1 — 핵심 루프 뼈대 (지금 시작할 부분)
- [ ] 동화 맵 1개 (고정 배경 일러스트, 임시 placeholder 가능 — 프롬프트 생성 아님)
- [ ] 캐릭터 1~2명과 텍스트 대화 (LLM 연동, 고정 페르소나 프롬프트)
- [ ] 맵 선택 화면 → 맵 진입 → 캐릭터 클릭 → 대화창 흐름
- [ ] 대화 중 선택지(2~3개 선택 가능한 대사/행동) 제공 → 선택에 따라 스토리 분기
- [ ] 진행된 대화·선택이 스토리 텍스트로 누적 저장되는 최소 구조

### Phase 2 — 캐릭터 도감(선택 기능) & 스토리 분기 확장
- [ ] 캐릭터 도감 UI (스프라이트 + 이름 + 성격 프리셋)
- [ ] "캐릭터가 필요한 순간" 트리거 → 도감에서 선택 → 맵에 배치
- [ ] 도감 캐릭터도 대화 가능하도록 페르소나 연결
- [ ] 선택지 분기가 이후 씬/대화 내용에 실제로 영향을 주도록 스토리 상태 관리 확장

### Phase 3 — 그림 스캔 (선택 가능한 추가 기능, 나중에)
- [ ] 카메라 촬영 → 배경 제거 → 스프라이트화 파이프라인
- [ ] 이름/성격 온보딩 → LLM 페르소나 시드 저장
- [ ] procedural 애니메이션(리깅 없이) 적용

### Phase 4 — 결과물/부가 기능
- [ ] 음성 TTS
- [ ] 스토리 자동 기록 → 그림책 형태 결과물 생성
- [ ] 부모 대시보드

### Phase 5 — 향후 확장 (지금은 착수하지 않음)
- [ ] 프롬프트 기반 맵 생성 — 아이가 텍스트로 새 맵을 생성하는 기능(WrightHere의 실시간 3D 생성과 유사한 컨셉). Phase 1~4의 고정 맵 구조가 안정된 뒤 별도로 설계·구현.

## 코딩 컨벤션 / 주의사항

- Phase 1은 **3D, 실시간 이미지 생성, 프롬프트 기반 맵 생성, 그림 스캔 없이** 돌아가는 걸 목표로 한다. 이 범위를 벗어나는 기능을 먼저 만들지 말 것.
- 맵은 항상 고정된(사전 제작) 리소스로 다룬다. "프롬프트 입력 → 맵 실시간 생성" 관련 코드/UI는 Phase 5 전까지 착수하지 않는다.
- 캐릭터 대화는 자유 텍스트만으로 끝내지 말고, 선택지 기반 분기 구조를 함께 고려해서 설계한다 (대화 UI를 만들 때부터 선택지 컴포넌트를 염두에 둘 것).
- 캐릭터 스프라이트 애니메이션에 스켈레탈 리깅을 시도하지 말 것 (설계상 의도적으로 배제됨, 문서의 댓글 스레드에서 확정 예정 — 확정 전까지는 procedural 애니메이션만 구현).
- 동화 원작 캐릭터의 비주얼을 디자인할 때 특정 스튜디오 IP를 참고 이미지로 쓰지 말 것.
- 아동 대상 서비스이므로 LLM 응답에는 항상 안전 필터 레이어를 거치게 한다 (직접 연동 전 이 레이어부터 구현).

## 아직 열려있는 질문 (구현하면서 함께 결정)

- 타겟 연령 및 입력 방식 (초등 중고학년 9~12세, 텍스트+음성 하이브리드 입력을 기본 가정으로 두되 개발하면서 확정)
- 캐릭터 도감 규모 (맵당 몇 명, 오마주:오리지널 비율)
- "캐릭터가 필요한 순간"을 아이가 직접 트리거할지, AI가 스토리 흐름상 제안할지
- 도감 캐릭터와 내가 그린 캐릭터가 같은 화면에 섞였을 때 상호작용 설계
- 첫 번째로 만들 동화 1개의 구체적 선정 (맵/캐릭터/스토리 분기)
